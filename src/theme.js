import { darkTheme } from "naive-ui";
export const theme = darkTheme;
/**
 * @type import('naive-ui').GlobalThemeOverrides
 */
export const themeOverrides = {
    common: {
        primaryColor: '#ff66ab',
        primaryColorHover: '#ff66ab',
        primaryColorPressed: '#c54f86',
        bodyColor: '#382e32',
        baseColor: '#2a2226',
        fontFamily: '"Torus Pro", v-sans, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol"',
        fontWeight: '400',
        fontWeightStrong: '600',
        fontSize: '18px',
    },
};
